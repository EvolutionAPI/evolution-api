version: '3.3'

networks:
  evolution-net:
    driver: bridge

services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - 6379:6379
    networks:
      - evolution-net

volumes:
  evolution_redis:
